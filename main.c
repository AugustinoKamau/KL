#include <stdio.h>
#include <stdlib.h>
#include "scores.h"

int main(void)
{
	int arr[10];
	int result = read_scores(arr,10);

	if (result == EXIT_FAILURE)
	{
		return EXIT_FAILURE;
	}










	return EXIT_SUCCESS;
}
