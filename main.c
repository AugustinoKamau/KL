#include <stdio.h>
#include <stdlib.h>

int main(void){
        int x;
        int y;
        scanf("%d",&x);
        scanf("%d", &y);
        int sum = y + x;
        printf("The sum of x + y = %d", sum);
        return EXIT_SUCCESS;
}
